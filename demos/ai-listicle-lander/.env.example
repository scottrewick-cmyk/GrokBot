# Optional. Template mode works with no key (offline).
# When set, the Generate button can rewrite copy via OpenAI.
OPENAI_API_KEY=
# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
